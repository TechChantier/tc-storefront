TCPOS_API_BASE_URL=http://tcpos.local
TCPOS_TENANT_ROOT_URL=http://tcpos.local
# TCPOS_TENANT_HOST_MODE=single
STOREFRONT_SERVICE_TOKEN=abc
STOREFRONT_RESOLVE_REVALIDATE_SECONDS=60

# Production (Docker): copy this to /var/www/tc-storefront/.env on the server.
# Secrets are read at container start, not baked into the image.
